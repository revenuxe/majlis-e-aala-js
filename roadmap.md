# MAJLISE AALA — Task Roadmap

## Active
- [x] Plan flow: "Choose a Package" / "Build My Own Menu" → neutral unselected state, Recommended badge not pre-selected.
- [x] Plan flow: add "Custom" guest-count chip + input to the Date & Guests step.
- [ ] Homepage QuickPlanner: remove veg/non-veg + serving-style questions; simplify to guest count + single CTA → /plan. Fix odd layout (pill imbalance, bottom-nav overlap).
- [ ] Plan flow: add a Preferences step (food preference + serving style) so those questions live in the wizard, not the homepage.
- [ ] Plan flow: "What's the occasion?" → 2 cards per row (vertical image cards).
- [ ] Plan flow: "How would you like to plan?" (Package vs Menu) → single card per row (full-width).
